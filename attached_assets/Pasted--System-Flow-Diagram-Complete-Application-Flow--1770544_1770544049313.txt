# 📊 System Flow Diagram

## Complete Application Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                          USER JOURNEY                                │
└─────────────────────────────────────────────────────────────────────┘

1. USER AUTHENTICATION
   ┌──────────┐
   │  User    │
   │  Visits  │
   │ Frontend │
   └────┬─────┘
        │ Clicks "Login with Discord"
        ▼
   ┌──────────┐     OAuth      ┌──────────┐
   │ Supabase ├───────────────►│ Discord  │
   │   Auth   │                │   OAuth  │
   └────┬─────┘                └──────────┘
        │ Returns token
        ▼
   ┌──────────┐
   │ Callback │───► Redirects to /test ✅
   │  Handler │     (NOT to home!)
   └──────────┘


2. TEST SUBMISSION
   ┌──────────┐
   │   User   │
   │  Takes   │
   │   Test   │
   └────┬─────┘
        │ Submits answers
        ▼
   ┌──────────────┐
   │   FastAPI    │
   │    Server    │
   │ (unified)    │
   └───────┬──────┘
           │
           ├──► Stores submission
           │
           └──► Sends webhook ─────────┐
                                        │
                                        ▼
                              ┌────────────────┐
                              │  Discord       │
                              │  Channel       │
                              │                │
                              │  ┌──────────┐  │
                              │  │ Embed    │  │
                              │  │ Message  │  │
                              │  ├──────────┤  │
                              │  │ ✅ Approve│  │
                              │  │ ❌ Deny   │  │
                              │  └──────────┘  │
                              └────────────────┘


3. ADMIN APPROVAL (TWO PATHS)

   PATH A: Web Admin Panel          PATH B: Discord Buttons
   ┌──────────────┐                 ┌──────────────┐
   │    Admin     │                 │    Admin     │
   │   Visits     │                 │   Clicks     │
   │  /admin      │                 │   Button     │
   └──────┬───────┘                 └──────┬───────┘
          │ Clicks Accept                  │ Click ✅
          ▼                                ▼
   ┌──────────────┐                 ┌──────────────┐
   │ POST /api/   │                 │ POST /api/   │
   │ admin/action │                 │webhook/action│
   └──────┬───────┘                 └──────┬───────┘
          │                                │
          └────────────┬───────────────────┘
                       │ Both paths lead to...
                       ▼
                ┌──────────────┐
                │   Update     │
                │  Submission  │
                │   Status     │
                └──────┬───────┘
                       │
                       ▼
                ┌──────────────┐
                │   Call Bot   │
                │   API to     │
                │ Assign Role  │
                └──────────────┘


4. ROLE ASSIGNMENT
   ┌──────────────┐
   │  Discord Bot │
   │   (port      │
   │   8003)      │
   └──────┬───────┘
          │ POST /api/assign-role
          ▼
   ┌──────────────┐
   │   Discord    │
   │    API       │
   │  Add Role    │
   └──────┬───────┘
          │
          ├──► Add "Verified Staff" role to user
          │
          └──► Send DM to user ────────┐
                                        │
                                        ▼
                                  ┌─────────────┐
                                  │    User     │
                                  │  Receives   │
                                  │ Congrats DM │
                                  └─────────────┘
```

---

## Component Interaction Diagram

```
┌────────────────────────────────────────────────────────────────┐
│                      FRONTEND (React)                          │
│  Port: 3000                                                     │
│                                                                 │
│  Pages:                                                         │
│  ├─ Login.js ────────► Initiates Discord OAuth                │
│  ├─ Callback.js ─────► Handles auth, redirects to /test       │
│  ├─ Test.js ──────────► Takes test, submits answers           │
│  └─ Admin.js ─────────► Reviews & approves submissions        │
└───────────────────┬────────────────────────────────────────────┘
                    │
                    │ HTTP Requests
                    │
┌───────────────────▼────────────────────────────────────────────┐
│                  BACKEND (FastAPI + Discord Bot)               │
│  Port: 10000 (FastAPI) + 8003 (Bot API)                       │
│                                                                 │
│  unified_server.py                                             │
│  ├─ GET  /health ────────► Health check                       │
│  ├─ GET  /api/submissions ► Get all submissions               │
│  ├─ POST /api/submissions ► Create submission + webhook       │
│  ├─ POST /api/admin/action ► Admin approval (web)            │
│  └─ POST /api/webhook/action ► Admin approval (Discord)      │
│                                                                 │
│  discord_bot.py                                                │
│  └─ POST /api/assign-role ► Assign Discord role               │
└───────────────────┬────────────────────────────────────────────┘
                    │
                    │ API Calls
                    │
┌───────────────────▼────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                            │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │   Supabase   │  │   Discord    │  │   Discord    │        │
│  │     Auth     │  │   Webhook    │  │     API      │        │
│  │              │  │              │  │              │        │
│  │  - OAuth     │  │  - Embeds    │  │  - Roles     │        │
│  │  - Sessions  │  │  - Buttons   │  │  - Members   │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow

```
1. TEST SUBMISSION DATA FLOW
   ┌─────────────┐
   │ User Answer │
   └──────┬──────┘
          │
          ▼
   ┌─────────────────────────┐
   │ TestSubmissionCreate    │
   │ - user_id               │
   │ - username              │
   │ - answers[]             │
   │ - score                 │
   │ - passed                │
   └──────┬──────────────────┘
          │
          ▼
   ┌─────────────────────────┐
   │ Stored Submission       │
   │ - id (UUID)             │
   │ - status: "pending"     │
   │ - created_at            │
   │ - updated_at            │
   └──────┬──────────────────┘
          │
          ├──► In-Memory Array (submissions_db)
          │
          └──► Discord Webhook Payload
               ┌────────────────────┐
               │ Embed              │
               │ - Title            │
               │ - Fields           │
               │ - Color            │
               │ - Timestamp        │
               │                    │
               │ Components         │
               │ - ✅ Approve button│
               │ - ❌ Deny button   │
               │ - 🔗 Admin link    │
               └────────────────────┘


2. APPROVAL DATA FLOW

   Admin Action (Web):
   ┌─────────────────────────┐
   │ AdminAction             │
   │ - submission_id         │
   │ - action: "accepted"    │
   └──────┬──────────────────┘
          │
          ▼
   Update submission ───► status = "accepted"
          │
          ▼
   ┌─────────────────────────┐
   │ assign_discord_role()   │
   │ - user_id               │
   │ - role_name             │
   └──────┬──────────────────┘
          │
          ▼
   Discord Bot assigns role

   
   Webhook Action (Discord):
   ┌─────────────────────────┐
   │ WebhookInteraction      │
   │ - submission_id         │
   │ - action: "approve"     │
   │ - admin_user_id         │
   └──────┬──────────────────┘
          │
          ▼
   Same flow as above ────► Role assigned
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     ERROR SCENARIOS                          │
└─────────────────────────────────────────────────────────────┘

1. Bot Not Connected
   User submits test
        │
        ▼
   Webhook sent ✅
        │
        ▼
   Admin approves
        │
        ▼
   Try assign role ───► Bot offline ───► Return error
        │                                  │
        │                                  ▼
        │                            Log error
        │                            Show admin message
        │
        └──► Retry when bot reconnects


2. User Not in Server
   Admin approves
        │
        ▼
   Call bot API
        │
        ▼
   Bot checks ───► User not found ───► Return 404
        │                                │
        │                                ▼
        │                          "User must join server"
        │                          Show in admin panel
        │
        └──► Admin can retry later


3. Bot Permission Issue
   Bot tries to assign role
        │
        ▼
   Discord API ───► 403 Forbidden ───► Bot lacks permission
        │                                │
        │                                ▼
        │                          Log: "Need Manage Roles"
        │                          Return error to admin
        │
        └──► Fix: Move bot role higher


4. Webhook Failure
   Submission created
        │
        ▼
   Send webhook ───► Network error ───► Log error
        │                                Continue anyway
        │
        └──► Submission still in admin panel ✅
```

---

## Security Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                           │
└─────────────────────────────────────────────────────────────┘

1. AUTHENTICATION
   ┌──────────────┐
   │   User       │
   └──────┬───────┘
          │
          ▼
   Discord OAuth ────► Supabase validates
          │
          ▼
   Session token ────► Stored in browser
          │
          ▼
   Every request ────► Token verified


2. AUTHORIZATION (Admin Check)
   Request to /admin
          │
          ▼
   Extract Discord ID from session
          │
          ▼
   Check against ADMIN_USER_IDS
          │
          ├──► Match ────► Allow access ✅
          │
          └──► No match ─► Deny access ❌


3. WEBHOOK VALIDATION
   Discord webhook
          │
          ▼
   Environment variable ────► URL kept secret
          │
          ▼
   HTTPS only ────► Encrypted transmission
          │
          ▼
   Discord server validates sender


4. BOT TOKEN SECURITY
   Bot token in env vars
          │
          ▼
   Never in code ✅
          │
          ▼
   Render dashboard only
          │
          ▼
   Can be rotated anytime
```

---

## Deployment Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   DEPLOYMENT PIPELINE                        │
└─────────────────────────────────────────────────────────────┘

Local Development
     │
     ▼
Git commit & push
     │
     ▼
GitHub repository
     │
     ├──────────────────────┬──────────────────────┐
     │                      │                      │
     ▼                      ▼                      ▼
Render Backend        Render Frontend      Discord Bot
     │                      │                (runs on backend)
     │                      │                      │
     ▼                      ▼                      ▼
pip install          npm install             Started by
requirements.txt     package.json            start_all.sh
     │                      │                      │
     ▼                      ▼                      ▼
Run start_all.sh     npm run build           Connects to
     │                      │                 Discord API
     │                      ▼                      │
     │               Serve build/                  │
     │                      │                      │
     ▼                      ▼                      ▼
API running         Static site           Bot online
Port 10000          Port 3000             Port 8003 (internal)
     │                      │                      │
     └──────────────────────┴──────────────────────┘
                            │
                            ▼
                     Application Live! 🎉
```

---

## Monitoring & Logs

```
What to Monitor:

Backend Logs (Render):
  ├─ "🤖 Starting Discord bot..."
  ├─ "[BOT] Bot has connected to Discord!"
  ├─ "🌐 Starting FastAPI server..."
  ├─ "Application startup complete"
  ├─ "Discord webhook sent successfully..."
  └─ "Role assigned successfully to user..."

Frontend Logs (Render):
  ├─ "Build succeeded"
  ├─ "npm run build"
  └─ "Serving build/ on port 3000"

Discord Bot:
  ├─ Online status (green dot)
  ├─ Response to commands
  └─ DM messages sent

Health Endpoints:
  ├─ GET /health ─────► {"status": "healthy"}
  ├─ Frontend load ───► React app renders
  └─ Bot online ──────► Shows in member list
```

---

This visual guide shows exactly how all components work together! 🎯